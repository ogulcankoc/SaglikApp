<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Alarm kurmak için gerekli izinler -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" /> <!-- Android 12+ (API 31+) için tam zamanlı alarm izni -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" /> <!-- ⚠️ USE_EXACT_ALARM kaldırıldı (Google Play reddini önlemek için) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- Android 13 (API 33+) için bildirim izni -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Pil tasarrufu modunda alarmların çalışması için -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" /> <!-- Telefon yeniden başladığında alarmları kurabilmek için -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.flash" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaglikApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.HeartRateActivity"
            android:exported="false" />
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.WaterHistoryActivity"
            android:exported="false" />
        <activity android:name=".ui.WaterActivity" />
        <activity
            android:name=".ui.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.SleepActivity"
            android:exported="false" />
        <activity
            android:name=".ui.BmiActivity"
            android:exported="false" />
        <activity
            android:name=".ui.WelcomeActivity"
            android:exported="false" />
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- AlarmManager tetikleyince çalışacak BroadcastReceiver -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="false" /> <!-- Telefon yeniden başladığında alarmları kuracak Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- Bazı HTC cihazlar için -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- LiteRT-LM GPU Hızlandırması için Gerekli -->
        <meta-data
            android:name="com.google.android.gms.tflite_dynamite_module"
            android:value="com.google.android.gms.tflite_gpu" />
    </application>

</manifest>